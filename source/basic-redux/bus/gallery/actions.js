// Types
import { SHOW_PREVIOUS_PHOTO, SHOW_NEXT_PHOTO } from "./types";

export const showPreviousPhoto = {
    type: SHOW_PREVIOUS_PHOTO,
};

export const showNextPhoto = {
    type: SHOW_NEXT_PHOTO,
};
